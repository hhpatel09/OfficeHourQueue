%h1 USER INDEX
- if @enrollments.present?
  %table#enrollments{class: 'mdl-data-table mdl-js-data-table mdl-shadow--2dp highlight'}
    %thead
      %tr
        %th User Name
        %th Email
    %tbody
      - @users.each do |u|
        %tr
          %td{class:'mdl-data-table__cell--non-numeric'}=u.first_name + " " + u.last_name
          %td{class:'mdl-data-table__cell--non-numeric'}=u.email
  %br
  = button_to 'Edit', edit_enrollment_path(@c_id), { :method => :get, class: 'waves-effect waves-light btn-small grey'}
- else
  %p No enrollments to modify

%div{class: 'fixed-action-btn horizontal', style: 'bottom: 45px; right: 24px;'}
  = link_to "/enrollment/#{@c_id}/new", {class: 'btn-floating btn-large waves-effect waves-light blue lighten-2', id: 'add_question'} do
    %i{class: 'material-icons', style: 'font-size: 30px'} add

